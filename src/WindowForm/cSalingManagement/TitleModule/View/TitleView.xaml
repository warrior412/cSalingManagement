<UserControl x:Class="TitleModule.View.TitleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="600">
    <Grid Background="{StaticResource TitleBackgroundColor}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="105"/>
        </Grid.ColumnDefinitions>
        <DockPanel Grid.Column="2">
            <Button Style="{StaticResource btn-primary}" Height="30" Width="30" Padding="0"  Content="_" Command="{Binding MinimizedCommand}" Margin="0,0,5,0" />
            <Button Style="{StaticResource btn-success}" Height="30" Width="30" Padding="0"  Content="↑" Command="{Binding ExpandCommand}" />
            <Button Style="{StaticResource btn-danger}" Height="30" Width="30" Padding="0"  Content="X"  Command="{Binding CloseCommand}" />
        </DockPanel>
        
    </Grid>
</UserControl>
