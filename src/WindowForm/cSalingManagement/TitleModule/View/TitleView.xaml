<UserControl x:Class="TitleModule.View.TitleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="600">
    <UserControl.Resources>
        <ResourceDictionary>

            

            <!-- Put your not shared resource here -->

        </ResourceDictionary>
    </UserControl.Resources>
    <Grid Background="{StaticResource TitleBackgroundColor}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="50"/>
        </Grid.ColumnDefinitions>
        <Button Style="{StaticResource btn-danger}" Height="40" Width="40" Padding="0,5,5,5" Content="X" Grid.Column="2"/>
        <TextBlock Text="Hi, Admin" Style="{StaticResource H3}" Grid.Column="1"/>
    </Grid>
</UserControl>
