<Window x:Class="cSalingManagement.View.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainView" Height="500" Width="800" WindowStyle="None"  ResizeMode="CanResizeWithGrip"
        xmlns:prism="http://www.codeplex.com/CompositeWPF">
    <!--<Window.Resources>
        <DataTemplate DataType="{x:Type ViewModel:ProductDetailViewModel}">
            <View:ProductDetailView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type ViewModel:ProductListViewModel}">
            <View:ProductListView/>
        </DataTemplate>
    </Window.Resources>-->
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20*" MaxWidth="300" MinWidth="200"/>
            <ColumnDefinition Width="80*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.RowSpan="2">
            <ContentControl prism:RegionManager.RegionName="MenuRegion" />
        </Grid>
        <Grid Grid.Column="1">
            <ContentControl prism:RegionManager.RegionName="TitleRegion"/>
        </Grid>
        <DockPanel Grid.Column="1" Grid.Row="1" Background="WhiteSmoke" LastChildFill="True" >
            
        </DockPanel>
    </Grid>
</Window>
