<Window x:Class="DemoUserControl.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525"
        xmlns:myPopup="clr-namespace:MyPopUpControl;assembly=MyPopUpControl">
    <Window.Resources>
    </Window.Resources>
    <Grid>
        <Button Content="Button" HorizontalAlignment="Left" Margin="219,140,0,0" VerticalAlignment="Top" Width="75" Click="Button_Click"/>
        <myPopup:PopUpControl IsOpen="{Binding IsOpen,Mode=TwoWay}">
            <myPopup:PopUpControl.HostedContent>
                <ContentControl>
                    <TextBlock Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="20">
                            This is the first modal popup
                    </TextBlock>
                </ContentControl>
            </myPopup:PopUpControl.HostedContent>
            <Button Content="Button" Height="100" Width="75"/>
        </myPopup:PopUpControl>
        <Button Content="Button" HorizontalAlignment="Left" Margin="413,286,0,0" VerticalAlignment="Top" Width="75"/>
    </Grid>
</Window>
