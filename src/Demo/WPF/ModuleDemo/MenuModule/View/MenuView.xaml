<UserControl x:Class="MenuModule.View.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            <Button Content="Open Module 1" Grid.Row="0" Command="{Binding LoadModule1Command}"/>
            <Button Content="Open Module 2" Grid.Row="1" Command="{Binding LoadModule2Command}"/>
            <Button Content="Open Module 3" Grid.Row="2" Command="{Binding LoadSubModule1Command}"/>
        </Grid>
    </Grid>
</UserControl>
